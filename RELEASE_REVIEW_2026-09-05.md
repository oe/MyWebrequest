# 发布复核：暂不发布

日期：2026-09-05。审查基线为本地 `becf259`，工作区开始时干净；本轮另准备了商店文案与图片更新要求。
本报告不等同于当前安装包的真机签字，不代表已上传、推送或部署。

## 当前证据

| 项目                                     | 本轮结果                     | 对发布的意义                                                                                                  |
| ---------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------- |
| 格式、ESLint、TypeScript、209 项单元测试 | 通过                         | 代码层检查通过                                                                                                |
| 官网检查与构建                           | 通过，21 个文件无错误或警告  | 本地站点可构建                                                                                                |
| 浏览器产物审计、Firefox 包静态检查       | 通过                         | 权限、语言、图标、源码映射和远程代码检查通过；不代表真机验收                                                  |
| 依赖安全审计                             | 通过                         | 仅保留已验证本地补丁覆盖的两个构建期 image-size 公告；不能据此宣称全面安全审计完成                            |
| 六语言商店元数据                         | 结构审计通过，已修正文案定位 | 改为两网址创建、范围选择、测试与简单编辑；权限隐私说明保留                                                    |
| 商店图片来源审计                         | 失败                         | 明确报错 `chrome screenshots do not come from the current release contents.`，旧图不得上传                    |
| 当前新交互与真实请求                     | 未完成                       | 两网址创建、原生域名替换、编辑、默认启用以及菜单布局仍缺当前包的安装验证                                      |
| 当前提交 CI                              | 未完成                       | GitHub master 和最新绿色 CI 为 `6756f065`；审查开始时本地比它领先 12 个提交                                   |
| 公网域名与 HTTPS                         | 可达                         | `https://request.forth.ink/zh-hans/` 返回 HTTP 200；Pages 配置 cname 正确、证书 approved、https_enforced=true |
| 公网内容版本                             | 仍是旧部署                   | 本轮 UI、官网调整尚未推送或部署；域名可达不能证明内容已同步                                                   |
| 商店门户、旧版签名升级                   | 待完成                       | 未进行上传或发布；Chrome 老用户升级后的数据保留还需要签名版本验证                                             |

最近绿色 CI：[33823446295](https://github.com/oe/MyWebrequest/actions/runs/33823446295)，对应旧提交而非本轮候选。

## 必须先补的验收

1. 当前包的真实创建／编辑流程：默认 Enabled、用户关闭后保存、权限拒绝／授权、保存和重开。
2. 精确与整站规则实际请求效果：路径参数保留、子域名与协议端口边界、循环冲突、停用和浏览器重启。
3. Chrome/Edge 最低 121 与 Firefox 最低 142 的最终提交 CI；不能复用旧提交认证记录。
4. 以当前包采集真实原图，生成官网头图、分享卡、商店五图和宣传图，通过来源审计及视觉检查。
5. 完整 release:package、安装包可复现性、相同包的签名升级验证与商店门户检查。

本任务先前收到 Computer Use 的扩展页面 URL 安全拒绝，且明确禁止更换控制入口绕过。
因此本轮未运行扩展 E2E、截图采集器或浏览器运行验证，也没有把它们计为通过。
当前可做的源码、打包、元数据与图片制作准备工作继续完成。

## 素材已准备到哪一步

已准备 [五图分镜与拍摄要求](store-assets/REFRESH_BRIEF.md)，六语言商店主文案和扩展简介已同步定位。
实际 PNG 尚未更新，现有截图、轮播、宣传图与官网图仍是旧素材。
下一步必须采集当前真实 UI；不能仅修改旧图的版本、时间或哈希来让审计通过。
